pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo Build Success'
            }
        }
        stage('Deploy') {
            steps {
                sh 'echo Deploy Success'
            }
        }
    }
}
